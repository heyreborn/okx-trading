[package]
name = "integration-tests"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
model = { version = "=0.1.0", path = "../crates/model" }
config = { version = "=0.1.0", path = "../crates/config" }
instrument = { version = "=0.1.0", path = "../crates/instrument" }
okx-client = { version = "=0.1.0", path = "../crates/okx-client" }
tokio.workspace = true
